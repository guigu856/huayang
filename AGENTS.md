本项目是一个视频创作系统呃包括一些呃制作视频的一些组件比如说呃下载视频处理视频渲染视频等等的工具还有一些呃相关的知识哦比如说skills呃角色提示词是专门用于各agent 呃在进行视频创作的时候可调度的一些资源的系统

## 2. 开发原则

### 2.1 简洁优先

- 不多实现任何未要求的功能。
- 不为一次性代码做抽象。
- 不做未要求的"灵活性"或"可配置性"。
- 不为不可能出现的场景写错误处理。
- 如果你写了 200 行但可以缩成 50 行，重写它。

问问自己："一个高级工程师会说这太复杂了吗？"如果是，简化。

### 2.2 精确改动

**只动你必须动的。只清理自己造成的混乱。**

- 不要"改进"相邻代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有风格，即使你平常写法不同。
- 发现无关的死代码，提一嘴——别删它。
- 修改造成了孤儿代码：删除你的改动导致不再使用的 import/变量/函数。
- 不要删除预先存在的死代码，除非被要求。

**检验标准：** 每一行修改都应能直接追溯到用户的需求。

### 2.3 目标驱动执行

**定义成功标准。循环直到验证通过。**

将任务转化为可验证的目标：
- "加验证" → "为无效输入写测试，然后让它们通过"
- "修 Bug" → "写一个能复现它的测试，然后让它通过"
- "重构 X" → "确保重构前后测试都能通过"

多步骤任务陈述简要计划：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
```

### 2.4 高内聚低耦合

新增或修改功能必须遵循高内聚低耦合：单一职责、依赖显式、边界不越位（见「放代码的边界」）。

### 2.5 不写多余的兜底与兼容

- 已明确不需要的旧方案，直接按新方案实现，不保留旧路径、不写兼容分支、不留降级开关。
- 例外：兜底 / 兼容是系统性设计要求时才保留，且需在对应设计文档中有明确依据。

### 2.6 注释只写最终形态

- 注释描述代码当前是什么，不写推导过程、迭代历史（如"废弃 X 改为 Y"）、对话痕迹。
- 改动背景放进提交信息或 PR 描述。
- 详见 `docs/rule/代码编写规范.md`。

### 2.7 代码逻辑只写最终形态

- 修改时按「这个单元的最终行为应该是什么」重写受影响的最小单元，不在上一版周围打补丁、加修正层（“先算错再修正”）。
- 新方案下不可能出现的输入 / 状态，删除对应分支；换存储 / 命名 / 格式方案后，读写只走新路径，不新旧双路径并存。
- 不再被使用的函数 / 参数 / 字段 / 标志位随本次改动删除；行为改了，类型注解 / 命名 / 提示词 / 文档同步改到最终形态。
- 检验标准：若某段逻辑只有知道上一版才能解释它为什么存在，它就是迭代残留，删除或重写。详见 `docs/rule/代码编写规范.md`。


---

---

## 5. 文档同步规则

修改以下内容必须同步对应文档：

- 新增环境变量 -> `.env` 说明与相关配置文档
- 新增 CLI 参数 / 脚本入口 -> 命令速查
- 修改 API 返回结构 -> 接口设计文档
- 新增外部服务依赖 -> 架构文档
- 新增资产类型 / 工具 -> 沉淀模块文档
- 修改设计决策 -> `docs/项目设计文档.md` 或 `docs/后端架构方案.md`

---

## 6. 安全规则

开始前检查：

```bash
git branch --show-current
git status --short --branch
```

禁止执行：

- `git reset --hard`
- `git clean -fd`
- `rm -rf` 大范围删除
- 覆盖用户未提交文件
- 大范围重写无关模块

---

## 7. 交接规则

完成修改后必须输出：

- 改了哪些文件
- 为什么改
- 跑了什么验证
- 还有什么风险
- 下一步建议

---

## 8. 项目规范

- 前端文字统一使用中文。
- 除必要的 log 外，前端不输出调试信息。
- 文档编写遵循 `docs/rule/文档编写规范.md`：只写最终形态，不写推导过程。
- 代码与注释遵循 `docs/rule/代码编写规范.md`：注释与代码逻辑只写最终形态，高内聚低耦合，不写多余兜底与兼容。

 PowerShell 默认输出把中文文档打成了乱码；用 UTF-8 方式读取文档。
- 中文提交信息防乱码：clone 后先设 `git config i18n.commitEncoding utf-8` 与 `git config i18n.logOutputEncoding utf-8`（git 配置不入库，每个 clone 需各自设置）；编辑器/终端统一 UTF-8；提交中文信息时用 `git commit -F <utf-8 文件>`（而非 `-m`），避免 shell 编码改写导致提交信息损坏。