# 2026-07-23 工作记录

## 将 Huayang 视频创作插件接入 WorkBuddy（MCP）
- 已将 huayang 配置写入 `~/.workbuddy/mcp.json`（stdio，command: `huayang-mcp`，args: []）。原文件为空 `{"mcpServers": {}}`，已合并写入不覆盖。
- 验证：用 mcp 官方客户端库做 stdio 握手，server `name=huayang version=1.28.1`，`TOOL_COUNT=40`、`RESOURCE_COUNT=31`、`TEMPLATE_COUNT=3`。
- 结论：集成链路已通，huayang 可作为 WorkBuddy 的 MCP 工具集使用（40 工具 + 共享知识 resources）。
- 下一步：用户在 WorkBuddy 连接器页点「信任」huayang 即可激活；激活后本会话 Agent 即可直接调用创作工具与共享知识。
- 备注：手写 Content-Length 帧的握手脚本会与服务端死锁（无输出卡住）；改用官方 `mcp` 客户端库（项目 uv 环境）握手正常。huayang-mcp 实为 `huayang-mcp.exe`（uv 打包）。

## 清空 huayang 插件数据
- 用户确认全部清空。已删除 `D:\project\video_create-plugin\output\plugin` 下全部 15 个数据条目（workflow.sqlite3、retrieval.sqlite3、objects/、knowledge/、materials/、bgm/、editor/、tasks/、validation/、package-audit-*、隐藏文件 .inspection-binding.key），并重建空目录。
- 数据根确认逻辑：HUAYANG_* 均未设置；rules/main-agent.md 存在 → 默认 output_root = output/plugin；~/.huayang 不存在（无第二数据根）。
- 坑：`find -exec rm -rf {} +` 在 Windows Git Bash 报 "environment is too large for exec()"，改用 `rm -rf <dir> && mkdir -p <dir>` 成功。

## output/ 根目录数据归属澄清
- output/ 根下除 plugin/ 外有 4 个目录：download/、editor/（仅 .gitkeep 空占位，组件默认输出）、materials/（素材获取组件直接产物：searches/ provenance/ downloads/ + editing_spec.json、preparation_package.json）、tmp-wheel-check/（huayang-0.1.0.whl 构建残留，可删）。
- 三类归属：plugin 编排层数据(output/plugin)、组件层 CLI 直接产物(output/materials|download|editor，不经 MCP)、构建残留(tmp-wheel-check)。
- 组件层默认输出约定 output/<name>；plugin 经 MCP 调度时把 output_dir 重定向到 output/plugin/<name>；output/ 整体被当作 media_roots 媒体输入边界。
